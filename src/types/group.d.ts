type NewGroupRequest = {
  name: string;
};

type GroupIdParmas = {
  groupId: string;
};

type GroupSystemRequest = {
  groupId: string;
  systemId: string;
};

type GroupUserRequest = {
  groupId: string;
  userId: string;
};
